1. 请将您的XML文件生成线上地址，并提交线上的XML地址，百度会自动抓取已经提交的资源地址。

2. 每个XML文件最多包含5000个url，并且文件在压缩前不得超过9.5MB，必须将其分割成多个XML文件，并提供索引文件。
<sitemapindex>
    <sitemap>
        <loc>
            http://www.baidu.com/sample.xml
        </loc>
    </sitemap>
</sitemapindex>
3. 提交前请校验XML的正确性，如果提交的是索引文件，请逐条校验单个XML文件。

4. 请尽量保证字段的完整性，字段的完整度会影响该条数据的展现。

5. 请将XML格式化后再提交。

